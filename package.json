{
  "name": "ai_invoice_generator",
  "version": "1.0.0",
  "description": "",
  "main": "build/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "node server/build/server.js",
    "build": "cd server && npm install && npm run build && cd ../client && npm install && npm run build"
  },
  "keywords": [],
  "author": "Vladimir Monarov",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
